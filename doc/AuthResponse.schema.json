{
  "$id": "https://github.com/nmelnick/ljrp/doc/auth.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AuthResponse",
  "type": "object",
  "properties": {
    "jwt": {
      "type": "string",
      "description": "The JWT to return in future responses"
    },
    "default_picture_url": {
      "type": "string",
      "description": "The URL of the default picture, if get_picture_urls was sent in the request"
    },
    "picture_keywords": {
      "type": "array",
      "description": "The keywords of the user pictures, if get_picture_keywords was sent in the request.  Picture keywords are used to identify which userpic (100x100 icon) to use for that particular post. For instance, the user may have 'Angry', 'Happy', and 'Sleepy' picture keywords which map to certain pictures. The client should also do a case-insensitive compare on this list when a mood is selected or entered, and auto-select the current picture keyword. That way it seems that selecting a mood also sets their corresponding picture",
      "items": {
        "type": "string"
      }
    },
    "picture_urls": {
      "type": "array",
      "description": "The URLs of the user pictures, if get_picture_urls was sent in the request. They correspond with the list of picture keywords returned. Note that the content behind these URLs can never change, so if your client downloads these to display, just cache them locally and never hit the servers again to re-download them or to even check if they've been modified",
      "items": {
        "type": "string"
      }
    },
    "friend_groups": {
      "type": "array",
      "description": "List of available friend groups",
      "items": {
        "$ref": "FriendGroup.schema.json"
      }
    },
    "journals": {
      "type": "array",
      "description": "List of journals (user, community, news) this user has permission to post in",
      "items": {
        "type": "string"
      }
    },
    "moods": {
      "type": "array",
      "description": "List of available moods, if get_moods was set to true in the auth request.  The contents are the new moods that have been added on the server since you last requested the list. Your client should cache the mood list on the client's computer to avoid requesting the whole list every time",
      "items": {
        "$ref": "#/definitions/Mood"
      }
    },
    "errors": {
      "type": "array",
      "description": "Error message(s), if failed",
      "items": {
        "type": "string"
      }
    }
  },
  "definitions": {
    "Mood": {
      "type": "object",
      "required": ["id", "name", "parent"],
      "properties": {
        "id": {
          "type": "number",
          "description": "The ID of this mood",
          "minimum": 0
        },
        "name": {
          "type": "string",
          "description": "Name of this mood"
        },
        "parent": {
          "type": "number",
          "description": "The parent mood ID for this mood",
          "minimum": 0
        }
      }
    }
  }
}
